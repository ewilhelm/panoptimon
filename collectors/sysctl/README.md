Reports on Linux system controls

# Config

The 'sysctls' configuration option contains a list of sysctls to query

Can use the module name ( 'vm' instead of 'vm.swappiness' )

# Returns:

The sysctl's value
